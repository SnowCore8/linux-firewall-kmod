# Kernel module build Makefile

# Define the module object (must match source filename)
obj-m := firewall.o

# Compiler flags
ccflags-y := -Wall

# Rename output after build
# The .ko will be named firewall.ko

