# firewall daemon configuration for frps protection
# Monitors frp log for suspicious connections

jails:
  frps:
    enabled: true
    log_files:
      - /root/frp/frp.log
    max_retries: 10
    findtime: 300
    ban_time: 3600
    regex: ""  # Uses built-in sshd pattern as fallback
