# Redis 防护配置
# 监控 Redis 未授权访问尝试，防护 Redis 暴力破解
# 注意：Redis 默认不记录认证失败，需在配置中启用 loglevel notice

jails:
  redis:
    enabled: true
    log_files:
      - /var/log/redis/redis-server.log
    # 智能推断参数：max_retries=3, findtime=300, ban_time=3600
    # 匹配 Redis 密码错误日志
    regexes:
      invalid_password:
        pattern: 'Client ([0-9]+\.[0-9]+\.[0-9]+\.[0-9]+).*invalid password'
