# Dovecot 邮件服务防护配置
# 监控 Dovecot 认证失败日志，防护邮件账户暴力破解

jails:
  dovecot:
    enabled: true
    log_files:
      - /var/log/mail.log
      - /var/log/dovecot.log
    # 匹配 Dovecot 认证失败日志
    regexes:
      auth_failed:
        pattern: 'auth:.*auth failed.*rip=([0-9]+\.[0-9]+\.[0-9]+\.[0-9]+)'
