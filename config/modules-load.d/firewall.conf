# Firewall kernel module auto-load configuration
# Install to: /etc/modules-load.d/firewall.conf
# This file tells systemd to load the firewall module at boot time.
# Module parameters are configured in /etc/modprobe.d/firewall.conf

firewall
