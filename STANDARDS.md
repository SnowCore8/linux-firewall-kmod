# 定级规范

> 本规范定义项目中所有问题、任务、风险的统一评级标准，确保评估一致性。

---

## 一、问题严重程度定级

### Critical（严重）

**定义**：导致系统崩溃、数据丢失、安全漏洞或核心功能完全失效的问题。

**判定标准**（满足任一即为 Critical）：
- 内核 panic / oops / 死锁
- 内存损坏（use-after-free、buffer overflow、double-free）
- 数据竞争导致的数据损坏（非理论风险，已有实际复现路径）
- 安全漏洞可被远程利用（RCE、权限提升、信息泄露）
- 核心功能完全不可用（封禁失效、日志解析完全错误）
- 数据丢失（SQLite 数据库损坏、配置丢失）

**响应时间**：立即修复，阻断其他开发工作  
**示例**：
- ddos_ban_worker 数据竞争导致封禁错误 IP
- 白名单子网链表 UAF 导致内核崩溃
- file_reader.rs 死代码导致文件读取逻辑混乱

---

### High（高）

**定义**：导致功能异常、性能严重下降或存在明确安全风险的问题。

**判定标准**（满足任一即为 High）：
- 功能异常但有 workaround（特定条件下触发）
- 性能下降 > 30%（在目标场景 10Gbps 下）
- 安全风险需要特定条件才能利用
- 资源泄漏（fd、内存）在长时间运行后会导致问题
- 接口不一致导致调用方容易出错
- 错误处理缺失导致静默失败

**响应时间**：当前迭代内修复  
**示例**：
- logger::get() Mutex 争用导致日志解析吞吐量下降 50%
- ban_ip_with_history 硬编码 jail_name 导致监控数据不准确
- SSE 连接无上限可被 DoS 攻击

---

### Medium（中）

**定义**：影响代码质量、可维护性或存在潜在风险但不立即影响功能的问题。

**判定标准**（满足任一即为 Medium）：
- 代码重复 > 50 行（违反 DRY）
- 性能下降 10-30%（非热路径或低频操作）
- 文档与代码不一致（可能误导用户）
- 测试覆盖缺失（重要功能无集成测试）
- 接口设计不合理但可通过文档说明规避
- 配置验证不足（可能加载无效配置但不崩溃）

**响应时间**：下一迭代修复  
**示例**：
- IPv4/IPv6 违规检测代码重复 80 行
- yaml-config.md 默认值与实际不一致
- 缺少 DDoS 检测集成测试

---

### Low（低）

**定义**：代码风格、命名、注释等不影响功能和性能的小问题。

**判定标准**（满足任一即为 Low）：
- 代码风格不符合规范（命名、缩进、注释）
- 冗余代码 < 10 行（无害但影响可读性）
- 文档拼写错误或表述不清
- 未使用的参数/变量（编译器警告级别）
- 性能影响 < 10%（微优化机会）

**响应时间**：空闲时修复或随其他变更一起修复  
**示例**：
- ntohl 重复调用 4 次（编译器会优化）
- 文档注释中英文重复
- 未使用的函数参数

---

## 二、任务优先级定级

### P0（最高优先级）

**定义**：必须立即处理的阻断性任务，不完成无法继续其他工作。

**判定标准**（满足任一即为 P0）：
- 修复 Critical 级别问题
- 阻断 CI/CD 流水线的问题
- 阻断发布流程的问题
- 安全漏洞应急响应
- 生产环境事故修复

**时间要求**：24 小时内开始，当前迭代必须完成  
**资源分配**：可中断其他任务，集中资源处理

---

### P1（高优先级）

**定义**：当前迭代必须完成的重要任务，影响核心功能或性能。

**判定标准**（满足任一即为 P1）：
- 修复 High 级别问题
- 核心功能性能优化（10Gbps 热路径）
- 功能正确性修复（影响监控数据准确性）
- 接口统一（影响开发效率）
- CI 测试覆盖缺失（影响质量保障）

**时间要求**：当前迭代内完成（通常 2-4 周）  
**资源分配**：正常开发流程，优先于 P2

---

### P2（中优先级）

**定义**：计划内的改进任务，提升代码质量和可维护性。

**判定标准**（满足任一即为 P2）：
- 修复 Medium 级别问题
- 文档修复和完善
- 测试补充（集成测试、端到端测试）
- 代码清理和重构（消除重复、改善可读性）
- 开发工具和流程改进

**时间要求**：下一迭代或空闲时完成  
**资源分配**：正常开发流程，按排期处理

---

### P3（低优先级）

**定义**：可选的优化和改进，不影响核心功能。

**判定标准**（满足任一即为 P3）：
- 修复 Low 级别问题
- 代码风格统一
- 微优化（性能提升 < 10%）
- 新功能的早期调研和原型
- 社区建设和文档翻译

**时间要求**：无明确期限，空闲时处理  
**资源分配**：仅在无 P0/P1/P2 任务时处理

---

## 三、性能影响定级

### 热路径（Critical Path）

**定义**：每秒执行 > 10,000 次的代码路径。

**特征**：
- netfilter 钩子函数（每个数据包）
- 日志解析主循环（每行日志）
- 哈希表查找/插入（封禁/解封操作）

**要求**：
- 任何改动必须 benchmark 验证
- 禁止引入堆分配（kmalloc/Box::new）
- 禁止引入锁争用（Mutex/spinlock）
- 禁止引入系统调用
- 性能回归 > 5% 即为 High 问题

---

### 温路径（Warm Path）

**定义**：每秒执行 100-10,000 次的代码路径。

**特征**：
- 定时任务（清理、同步）
- API 请求处理
- 配置重载

**要求**：
- 允许适度的锁和分配
- 性能回归 > 20% 即为 Medium 问题
- 应避免阻塞热路径

---

### 冷路径（Cold Path）

**定义**：每秒执行 < 100 次的代码路径。

**特征**：
- 模块初始化/清理
- 配置解析
- 日志输出

**要求**：
- 优先考虑正确性和可读性
- 性能优化为 P3 优先级
- 允许使用高级抽象

---

## 四、安全风险定级

### 严重（Critical Security）

**定义**：可被远程攻击者直接利用，无需认证或特殊条件。

**示例**：
- 远程代码执行（RCE）
- 权限提升（提权到 root）
- 敏感信息泄露（密钥、密码）
- 拒绝服务（无需认证的资源耗尽）

**响应**：立即发布安全补丁，通知用户升级

---

### 高危（High Security）

**定义**：需要特定条件或本地访问才能利用。

**示例**：
- 需要本地用户权限的提权
- 需要认证的敏感信息泄露
- 需要特殊网络位置的 DoS
- 配置注入导致命令执行

**响应**：当前版本修复，发布安全公告

---

### 中危（Medium Security）

**定义**：理论上的安全风险，实际利用困难。

**示例**：
- 竞态条件需要精确时序
- 信息泄露需要物理访问
- 配置错误导致的安全降级

**响应**：计划内修复，文档说明风险

---

### 低危（Low Security）

**定义**：防御纵深改进，非实际漏洞。

**示例**：
- 添加额外的输入验证
- 改进错误信息（避免信息泄露）
- 强化文件权限检查

**响应**：空闲时改进

---

## 五、定级流程

### 问题定级流程

1. **发现者**：初步定级，填写 Issue 模板
2. **维护者**：审核定级，调整或确认
3. **争议解决**：在项目会议中讨论，最终由项目负责人决定

### 任务定级流程

1. **提出者**：根据问题定级和建议优先级，创建任务
2. **计划会议**：团队讨论，确认优先级和排期
3. **调整机制**：新信息出现时可调整定级（需说明理由）

---

## 六、定级示例

### 示例 1：netfilter 数据竞争

**问题**：ddos_ban_worker 与热路径之间无同步  
**严重程度**：Critical（数据竞争可能导致封禁错误 IP）  
**任务优先级**：P0（阻断性缺陷）  
**性能影响**：热路径（每个数据包经过）  
**安全风险**：高危（可导致合法 IP 被封禁）

### 示例 2：logger::get() Mutex 争用

**问题**：每次日志调用获取 Mutex 锁  
**严重程度**：High（性能下降 50%）  
**任务优先级**：P1（核心功能性能优化）  
**性能影响**：热路径（每行日志解析）  
**安全风险**：无

### 示例 3：文档默认值不一致

**问题**：yaml-config.md 写 max_retries=5，实际为 3  
**严重程度**：Medium（误导用户配置）  
**任务优先级**：P2（文档修复）  
**性能影响**：无  
**安全风险**：无

---

## 七、定级原则

1. **基于事实**：定级应基于实际影响，而非理论风险
2. **避免过度防御**：不为理论上可能的攻击场景提升定级
3. **性能导向**：10Gbps 场景下的性能问题优先于其他场景
4. **用户视角**：影响用户体验的问题优先于内部代码质量
5. **动态调整**：新信息出现时应重新评估定级

---

**最后更新**：2026-06-17
