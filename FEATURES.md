# Web 前端功能清单

**更新日期**: 2026-06-28
**版本**: v2.2.1

---

## ✅ 已完整功能

### Dashboard (仪表盘)
- [x] 威胁等级显示 (Normal/Warning/Critical) + 因素标签
- [x] 实时流量趋势图 (LineChart)
- [x] 攻击源 TOP 10 列表
- [x] 协议分布饼图 (PieChart)
- [x] 封禁趋势图 (24H)
- [x] 内核统计 (丢包、通过、封禁表、白名单、运行时间)
- [x] SSE 实时数据推送 + 自动重连（指数退避 1s → 30s）
- [x] 骨架屏加载动画
- [x] 威胁状态栏布局优化
- [x] 24 小时攻击热力图（按小时聚合，颜色深浅表示攻击强度）
- [x] 最近封禁时间线（最近 8 条活跃封禁，IP/Jail/渐进式标记/剩余时长）
- [x] 快捷操作面板（一键封禁 TOP 5 攻击源 / 一键解封所有临时封禁）
- [x] Toast 通知系统（success/error/info 三种类型，3 秒自动消失 + 滑入动画）

### Bans (封禁管理)
- [x] 封禁原因分布图
- [x] 封禁趋势图 (24H)
- [x] 手动封禁表单
  - [x] IP 地址验证
  - [x] 时长范围验证 (0-86400 秒)
  - [x] 重复封禁检查
- [x] 解封操作 (带确认对话框)
- [x] 搜索过滤 (IP/Jail/原因)
- [x] 分页功能
- [x] 排序功能 (6 种排序方式)
- [x] 封禁时间格式化
- [x] 剩余时间倒计时显示
- [x] 封禁效果分析面板（按渐进式级别统计复发率 + 判定建议）
- [x] 威胁等级指示器（封禁列表行首颜色圆点：永久=红、×3+=橙、×2=黄、×1=透明）
- [x] 封禁详情弹窗（Jail/原因/失败次数/累计封禁/渐进式等级/下次封禁时长预测）
- [x] 封禁决策路径可视化（5 步决策路径：流量检测 → Jail 匹配 → 阈值判定 → 渐进等级 → 封禁决策）
- [x] IP 信誉分显示（分数/颜色/等级/阈值乘数）
- [x] 周期性攻击检测面板（TOP 20 周期性攻击者，规律度评分/平均间隔/抖动率）
- [x] 协同攻击检测面板（TOP 20 协同攻击事件，Jail/协同度/IP 列表）
- [x] 封禁时长推荐面板（per-Jail 推荐，基于复发间隔中位数分析）
- [x] 攻击源网络分布面板（TOP 20 /24 子网，唯一 IP 数/封禁数/占比条形图）
- [x] 信誉分面板（TOP 20 低信誉 IP 列表 + 进度条 + 评分规则说明）
- [x] 封禁时长 Histogram（≤60s / ≤5min / ≤1h / >1h 水平条形图）
- [x] 批量操作（多选封禁/解封、按 Jail 批量操作）

### Whitelist (白名单管理)
- [x] 白名单列表显示
- [x] 添加 CIDR 表单
  - [x] CIDR 格式验证
  - [x] 重复检查
- [x] 移除操作 (带确认对话框)
- [x] IPv4/IPv6 支持
- [x] 智能推荐（分析 BAN_HISTORY 识别 /24 子网 + 频繁临时封禁 IP，一键采纳）

### Jails (Jail 配置)
- [x] Jail 列表显示
- [x] 启用/禁用状态显示
- [x] 启用/禁用切换 (带确认对话框 + 乐观更新)
- [x] 封禁数统计
- [x] Jail 封禁分布图
- [x] per-Jail 运行时统计（正则匹配率/解析行数/提取 IP 数/失败尝试/触发封禁）
- [x] 失败阈值显示（高峰期 `5→8` 格式，原值→有效值）
- [x] 阈值放宽策略信息（"高峰期 ×1.5 · 内网 ×2.0"）
- [x] 阈值调优建议面板（方向标签 + 当前→推荐值 + 说明文案，基于 7 天复发率分析）

### DDoS Monitor (DDoS 监控)
- [x] 威胁等级显示
- [x] 实时流量监控
- [x] 协议分布图
- [x] 阈值对比表
- [x] 流量趋势图 (最近 5 分钟)
- [x] 攻击源 TOP 10
- [x] 协议异常雷达图（6 轴 SVG：SYN/UDP/ICMP/ACK/RST/FIN，异常颜色编码）
- [x] UDP 端口分布面板（TOP 20 端口，端口号/数据包/字节/最后出现时间）
- [x] ICMP 类型分布面板（TOP 15 类型，自动识别常见 ICMP 类型名称）
- [x] 包大小分布直方图（5 桶：\<64B / 64-256B / 256B-1KB / 1-1.5KB / >1.5KB，颜色编码）
- [x] TTL 分布直方图（6 桶：=1 / 2-32 / 33-64 / 65-128 / 129-192 / 193-255，颜色编码）
- [x] IP 分片统计面板（分片包数条形图 + 分片比例 + 状态指示）
- [x] 端口扫描检测面板（TOP 20 扫描者，严重程度颜色编码：5-20/20-50/>50 端口）
- [x] 服务探测检测面板（TOP 20 探测者，协议类型数 + 数据包数）

### Logs (系统日志)
- [x] 日志级别过滤 (ERROR/WARN/INFO/DEBUG)
- [x] 关键词搜索
- [x] 流式日志 (SSE 实时推送 tail -f 语义)
- [x] 暂停/继续控制
- [x] 分页加载
- [x] 日志轮转检测（copytruncate + mv+create 两种模式）

### Settings (设置)
- [x] 守护进程信息 (版本、运行时间、统计)
- [x] 内核模块信息 (容量、当前值)
- [x] WebUI 配置编辑
  - [x] SSE 推送间隔
  - [x] 速率警告/严重阈值
  - [x] SYN 警告/严重阈值
- [x] 配置保存 (带成功/失败提示)
- [x] 关于信息 (项目、许可证、仓库、技术栈)
- [x] SSE 连接状态诊断（连接数/上限 + 超限提示）

---

## ✅ 后端核心功能

### 封禁系统
- [x] 渐进式封禁（第 1 次基础时长 → 第 2 次 30min → 第 3 次 24h → 第 4 次+ 永久）
- [x] 封禁历史持久化（SQLite `ban_history` 表，启动自动加载，7 天过期清理）
- [x] 封禁时长 Histogram（4 桶累积计数，所有解封路径同步记录）
- [x] 封禁事件追踪（SQLite `ban_events` 表，每次封禁事件记录，7 天过期清理）

### IP 信誉分系统
- [x] 评分规则（初始 100，失败 -10，封禁额外 -10，每小时 +1 恢复，范围 0-100）
- [x] 阈值联动（≥80 → ×1.0，50-79 → ×0.8，<50 → ×0.5）
- [x] SQLite 持久化（`ip_reputation` 表，启动自动加载）
- [x] Prometheus 指标（3 个：tracked_ips / low_count / critical_count）

### 智能阈值
- [x] 按服务类型（Jail YAML 配置：SSH=3, Nginx=10, Redis=3 等）
- [x] 按时间段（业务高峰期 9-18 UTC 自动 ×1.5）
- [x] 按来源（内网 IP ×2.0，与高峰期叠加）
- [x] 按信誉（信誉分乘数 ×0.5/×0.8/×1.0，三种策略叠加）

### 攻击分析
- [x] 周期性攻击检测（ban_events 表 + 变异系数 CV 算法，评分 0-100）
- [x] 协同攻击检测（5 分钟滑动窗口，同 Jail 内 ≥3 IP 协同，评分 0-100）
- [x] 封禁效果分析（按级别统计复发率，判定阈值松紧）
- [x] 封禁时长推荐（基于复发间隔中位数，per-Jail 推荐）
- [x] 阈值调优建议（基于 7 天复发率，per-Jail 推荐调整方向）
- [x] 攻击源网络分布（/24 子网聚合，TOP 50）
- [x] 白名单智能推荐（/24 子网 + 频繁临时封禁 IP 模式识别）

### 流量特征分析（内核态）
- [x] TCP 异常标志位检测（SYN+FIN/SYN+RST/NULL scan 内核层直接丢弃）
- [x] UDP 端口分布（256 桶哈希表，procfs/API/Web UI 全链路）
- [x] ICMP 类型分布（64 桶哈希表，IPv4+IPv6，全链路可视化）
- [x] 包大小分布（5 桶 atomic64 直方图，无锁递增）
- [x] TTL 分布（6 桶 atomic64 直方图，IPv4 TTL + IPv6 HopLimit）
- [x] IP 分片检测（2 计数器：分片包数/总 IP 包数，IPv4 frag_off + IPv6 分片扩展头）
- [x] 端口扫描检测（per-IP unique_ports 计数器，阈值 ≥5 端口）
- [x] 服务探测检测（per-IP 协议多样性，阈值 ≥3 种协议）

### DDoS 检测
- [x] 多窗口 EWMA 检测（短期 5s / 中期 60s / 长期 300s）
- [x] 动态基线学习（EWMA α=0.01，netlink 同步到内核）
- [x] 异常基线冻结（流量 > 基线 ×3 时冻结 EWMA 5 分钟）
- [x] 协议专项检测（SYN/UDP/ICMP Flood + ACK/RST/FIN Flood）
- [x] DDoS 决策引擎（DdosDecisionEngine，内核自行决策封禁）

### 基础设施
- [x] Prometheus 指标（24 个：4 内核 + 12 用户态 + 4 netlink + 1 uptime + 3 信誉分）
- [x] SSE 长连接推送（axum 原生 SSE，连接数限制 10）
- [x] 日志 SSE 实时推送（tail -f 语义，连接数限制 5）
- [x] 配置热重载（SIGHUP + 持久化 + 版本历史 + 回滚）
- [x] netlink 双向通信（21 种消息类型，事件推送 + 请求-响应）

---

## 📊 功能完整度

| 模块 | 功能数 | 完成度 | 备注 |
|------|--------|--------|------|
| Dashboard | 13 | 100% | ✅ 含热力图/时间线/快捷操作/Toast |
| Bans | 19 | 100% | ✅ 含效果分析/决策图/周期性/协同/推荐/网络分布 |
| Whitelist | 6 | 100% | ✅ 含智能推荐 |
| Jails | 9 | 100% | ✅ 含运行时统计/阈值调优 |
| DDoS | 13 | 100% | ✅ 含雷达图/UDP/ICMP/包大小/TTL/分片/扫描/探测 |
| Logs | 6 | 100% | ✅ 含 SSE 实时推送 |
| Settings | 7 | 100% | ✅ 含 SSE 诊断 |
| 后端核心 | 35 | 100% | ✅ 渐进式/信誉/智能阈值/攻击分析/流量特征 |
| **总计** | **108** | **100%** | 🎉 |

---

## 🎯 下一步规划

### Phase 3 高级智能（待实现）
1. **异常检测**：无监督学习检测异常流量模式
2. **攻击预测**：基于历史模式预测下次攻击时间
3. **自动调优**：根据封禁效果自动调整阈值（强化学习）
4. **威胁情报集成**：对接外部威胁情报源（已知恶意 IP）

---

**报告生成**: 2026-06-28
**验证环境**: 本地测试环境 (Ubuntu 24.04, Kernel 6.x)
